export default async function getCoinParameters() {
  //  const data = await fetch('url'); //  api is unavailable
  //  const parameters = await data.json();

  //  this is just simulation of api
  const parameters = {
    ETH_blockRev: 4.18968596639766,
    ETH_diff: 5.407256463815861e15,
  };

  return parameters;
}
