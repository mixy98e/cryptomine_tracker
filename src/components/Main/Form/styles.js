//  [ styles.js ] --used for--> [ Form.jsx ]

import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  readioGroup: {
    display: "flex",
    justifyContent: "center",
    marginBottom: "-10px",
  },
  button: {
    marginTop: "20px",
    marginBottom: "20px",
  },
}));
